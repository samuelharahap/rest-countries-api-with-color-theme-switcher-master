import { Link } from 'react-router-dom'
import { Container } from '../utils/styled'

function Country() {
  return (
    <Container>
      detail
      <Link to="/">back</Link>
    </Container>
  )
}
export default Country
